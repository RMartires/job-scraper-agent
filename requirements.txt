# Core dependencies for job scraper agent
# Note: browser-use requires Python 3.11+
browser-use>=0.7.0
pydantic>=2.0.0
python-dotenv>=1.0.0
lmnr>=0.1.0
pymongo>=4.0.0
playwright>=1.40.0

# Standard library dependencies (no need to install)
# os, re, datetime, traceback, sys
